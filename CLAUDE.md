# figma-training-ozaki25

Figma未経験のエンジニア向けハンズオン教材（VitePress製）。2日間でタスク管理ツール2画面（一覧＋登録フォーム）のUIデザインから、Figma MCP + Claude Codeによるコード生成までを学ぶ。

カリキュラム方針・章立て・設計思想の正は **PLAN.md**。技術構成の決定事項は **NOTES.md**。タスク管理は **BACKLOG.md**。迷ったらまずこの3ファイルを読む。

## ブランチ運用

- **mainのみ**。draft/publish系の運用はしない
- コミットはmainに直接行う

## ディレクトリ構成

- `docs/lessons/` — レッスン本文を置く
  - 命名規則: `docs/lessons/<章>-<節>/index.md`（例: `docs/lessons/1-1/index.md`）。URLは `/lessons/1-1/` になる
  - レッスン専用の画像・SVGは `docs/public/lessons/<章>-<節>/` に置き、本文からは `/lessons/1-1/xxx.svg` のように絶対パスで参照する
- `docs/.vitepress/` — VitePress設定・テーマ
- `docs/public/` — 静的アセット（画像・SVGなど）

## 執筆ルール

### レッスンの単位

- 1レッスン = 15分・1テーマが原則。長くなるなら分割する
- ただし4-2/4-3のように「章の集大成」となるレッスンは20〜30分まで許容する（分割すると章の連続性が失われるため）
- 練習章（1〜3章）は同じサンプル（プロフィールカード）を前レッスンから引き継いで使う。各レッスン冒頭で「何を使うか」を明示する。省略形の代替手順（「手元にない場合は〜」）は要点が伝わらないので書かない
- 実践章（4〜6章）は同じ画面を複数レッスンで連続構築するため、前レッスンの成果物への依存を許容する。ただし各レッスン冒頭で「何を出発点として、何を追加するか」を1〜2文で明示する
- 演習（実際にFigmaで手を動かす）を中心に構成する。読むだけのレッスンにしない
- 演習中に必要になる操作の説明は、演習の手順の中に書く。「新出の操作」のような節に切り出さない（演習だけ読んでいる人に届かない）
- 演習の前に置くのは「何を目指すか」だけ（完成イメージ・構造・仕様表・設計判断の理由）。「作る順序」のような手順の要約や作り方の原則を節にして置かない。まだ手元に対象がない時点で抽象的な順序を読んでも参照先がなく伝わらないので、その説明は対象を操作する手順の中に書く（例: 「このフレームがサイドバーになる」）
- 手順には操作だけでなく「いま何を作っているのか」を一言添える。作る意味の分からない図形操作の羅列にしない
- 1ステップに操作を詰め込まない。操作が3つを超える長文になるなら、サブステップの番号リストに分ける
- 設定が4つ以上並ぶときは、要素を作った（包んだ）ステップの直下に箇条書きでぶら下げる。「次のとおりにする」のような中身のない前置き行は書かない
- 同じ操作は毎回同じ文型で書く。「`t` でテキスト `X`（…）を作る」「選択して `Shift + A` で縦に包む」「名前を `x` にする」「`Cmd + D` で N個にする」のように言い回しを固定し、読者が見た目で操作を判別できるようにする
- 各レッスン末尾に「ゴール確認」チェックポイントを置き、できたかどうかを自分で確認できるようにする
- ドリル（クイズ・小テスト）は作らない

### ゴール・まとめは短く

- チェックポイントやまとめの項目は3項目程度（多くても4）に絞る
- 各項目は1行・1文以内。本文の説明を繰り返さない
- 項目が増えそうなときは分割ではなく統合して数を減らす

### 読者への向き合い方

- 専門用語は初出時に平易な言葉で説明する。「知っていて当然」という態度を取らない
- Figmaの知識は一切前提にしない
- 読者の実際のHTML/CSS経験は浅い（新人エンジニアで実務経験は数ヶ月程度）。CSSへの読み替え（Fill container ≒ `flex: 1` 等）は、CSSを知っている読者への添え物として書いてよいが、それを理解しないと本文の説明が成立しない書き方（アナロジーが唯一の説明手段になっている状態）にはしない。Figma側の説明はCSS抜きでも単独で分かる形にする

### 実施状況に依存しない書き方

- 教材本文は特定の実施状況（講師配布・自習・チームでの共有など）に依存する記述を避ける
- ファイルの配布方法・アクセス経路・作成手順（「ホーム画面から新規ファイルを開く」「共有された教材ファイルを開く」「講師から配られた…」など）は書かない
- 読者は既にキャンバスにたどり着いている前提で、演習手順は最初の操作から書き始める

## 自然な日本語で書く

判定基準は「**同僚に口頭で説明するとき、その言い回しを使うか**」。使わない言い回しは避ける。不自然さの根本原因は英語の直訳調。伝えたい中身を、日本語話者が口頭で言うならどう言うかで書き起こす。

- **長い段落を作らない** — 3文以上続く説明は短文・箇条書き・表に分解する
- **ダッシュ（——）を文中に使わない** — 文を分けるか、カッコを使う
- **括弧書き・記号を多用しない** — 「A＝B」は「AはB」と言葉で書く。装飾的な括弧は文に溶かす
- **直訳調を避ける** — 「〜の欠如」のような硬い名詞化、「提供されています」のような不自然な受動態、「落とし穴」「柱」「器」のような比喩のラベル語を使わない（実際のレイヤー名や「白いカード」のような見たままの言葉で書く）
- **詩的な文体を避ける** — 体言止めの列挙、短文の連打、「なぜ〜なのか。答えは〜」の語り口、ドラマチックな締めの一文を使わない
- **説明の足場を残さない** — 「まず全体像を押さえると」「違いは大きく2つです」「〜が重要です」のようなメタな前置き・数え上げ宣言・教科書的な締めを削り、中身だけ残す
- **見出しは簡潔に** — 「正体」「魔法」「旅」のような詩的な単語を見出しに使わない。レッスンのタイトルはPLAN.mdの章立て名（「Figmaを開いてみる」等の動詞形も可）に従い、本文中の節見出しは名詞句を基本とする

## コンテンツの鮮度

- 執筆時点（2026年7月）のFigma UI・機能・Figma MCPの仕様に合わせて書く
- 古いメニュー名・手順を鵜呑みにせず、レッスン執筆前にWeb検索で現行のFigma UIを確認する
- 特に6章（Figma MCP + Claude Code）は変化が速いため、執筆直前に最新のセットアップ手順を確認する

## 図版・キャプチャ

- 完成イメージ・画面キャプチャは教材専用Figmaファイルからの実キャプチャを使う（2026-07決定）。
  リモート実行環境のプロキシがFigmaの画像ダウンロードを弾くため、Figma側の作成・編集はリモートで行い、
  `mcp__Figma__get_screenshot`によるキャプチャ取得だけはプロキシ制約のないローカルのClaude Codeに委譲する
- 演習の答え（.figファイルそのもの）は配布しない
- 概念図（Auto Layoutの挙動、MCP連携の流れなど、画面キャプチャではなく模式図で説明するもの）は
  **Mermaid禁止**。SVGを自作して `docs/public/` に置く
- Figma MCPを何らかの目的で使う場合の絶対ルール: 触れてよいのは
  `.claude/figma-config.local.json`（gitignore対象）に記録された教材専用ファイルだけ。
  他ファイルには絶対にアクセスしない
- **【絶対厳守】教材コンテンツに実在の個人情報（本名・メール・所属・アイコンなど）を入れない**。
  `whoami`等で取得したユーザー本人の情報を教材に転用しない。ダミー人物名は`Taro Yamada`など
  汎用サンプル名を使う（詳細はPLAN.md「教材コンテンツに個人情報を入れない」）

## サイトテーマ

- テーマカラーはFigmaブランド系の配色（紫 #A259FF などを軸に検討）
- ライト/ダーク両対応必須
- カラーコントラストはWCAG AAを満たすこと

## 開発コマンド

- `npm run docs:dev` — ローカルプレビュー
- `npm run docs:build` — 本番ビルド
- `npm run docs:lint` — textlintによる文章校正（対象は `docs/**/*.md`）
- `npm run docs:style` — 詩的見出し・足場フレーズ・全角記号・複文ゴール・AI出力前提の疑いの候補検出（CLAUDE.md方針の機械化lint）
- `npm run pwa:icons` — PWAアイコン生成

## 品質チェック

コミット前に必ず `npm run docs:build` と `npm run docs:lint` を通す。どちらかが失敗した状態でコミットしない。`docs:style` は候補検出なので誤検出を含む前提で、指摘があれば内容を人が判断する。

## 進め方

- BACKLOG.mdの1タスク = 1スプリント。実行 → レビュー → 振り返り → コミットで1周
- 振り返りの結果は必ずPLAN.md（恒久ルール）やBACKLOG.md（計画の見直し）に反映する
- 決定事項・進捗は必ずファイルに記録する。会話履歴だけに残さない
